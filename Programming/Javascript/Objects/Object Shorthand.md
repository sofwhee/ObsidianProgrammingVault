# Object shorthand

```javascript
// new way
const nowFancyObject = { name, age, color };

// old way
const name = "Bob";
const age = 28;
const color = "red";

const thatObject = { name: name, age: age, color: color };
```