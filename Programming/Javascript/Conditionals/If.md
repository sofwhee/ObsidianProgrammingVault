```javascript
if (hour < 18) {  
  greeting = "Good day";  
}
```

```javascript
if (x > 50) {
  /* do something */
} else if (x > 5) {
  /* do something */
} else {
  /* do something */
}
```

Change variables inside:

```javascript
if (read = true) {
	read = "yes"
} else
	read = "no"
}
```

Ternary operator

```javascript
 read = read ? "yes" : "no";
```