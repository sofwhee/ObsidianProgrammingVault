A templating language.

Called handlebars because you use handlebars to embed values. (looks like a mustache)

**JQuery = Given you have this html already, we're going to allow you to bind behaviours to it.**
**Handlebars = Here's how you generate the HTML.**

You write code that requires specific things.
And then you can feed it values from JS and it will handle all the registration there.

