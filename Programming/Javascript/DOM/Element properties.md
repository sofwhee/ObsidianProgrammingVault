## Navigation

`.parentNode`
`.children` = [[HTMLCollection]] (only element nodes) or empty list.
`.childNodes` = HTMLCollection (includes non-element nodes, like text and comments)
`.firstElementChild` `.lastElementChild` `.nextElementSibling` `.previousElementSibling`

## Attributes

`.classList` = DOMTokenList
`.className` = string

## Measurement

`.clientHeight` `.clientWidth`
`.clientLeft` `.clientRight` `.clientTop`