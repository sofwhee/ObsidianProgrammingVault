Defers exact steps of an algorithm to a subclass.

You could define a rough skeleton of an algorithm, or data structure in an abstract class, but allow the subclasses to override certain parts of that algorithm or data structure without altering the algorithm's overall structure.