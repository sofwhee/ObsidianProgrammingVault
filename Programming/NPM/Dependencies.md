Two properties are added to `package.json` when a package is installed:
- `devDependencies`: Packages that are only necessary during development (test runners, linters)
- `dependencies`: Dependencies needed in both development and production

With packages, you can:
`--save-dev`
`--save`

