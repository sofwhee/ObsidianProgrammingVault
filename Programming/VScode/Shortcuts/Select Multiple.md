**Ctrl + D** 
selects the current word and then selects the next occurrence of that word with each subsequent press. 

**Shift + Ctrl + L** 
Add multiple cursors to all occurrences of the current selection.