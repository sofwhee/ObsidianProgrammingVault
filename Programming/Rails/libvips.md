#Rails #packages

### check if installed: 
`sudo apt list libvips` (You should see a list of available versions.)

or

`sudo apt install libvips` (if already installed, you'll see a message saying so.)