 - They're just looking to reconfirm info
 - We want to compile Stream, GA4, and social media into a unified dashboard
 - It would help us track efforts vs performance
 - Therefore... They are requesting...
 - In the video provider data exports we generate...
 - ... they would like to have tags and categories included in them.
 - Otherwise, how can we get that information?

- Because they don't have tags and categories included in the vid provider data exports from our backend
- They are having a harder time tracking efforts vs performance.
- They are asking if we can provide it, or if there is another way to get that information.
- Note: they are still in the legacy platform

Our answer was to give them the ASHA Stream API documentation so they could use the API key to make requests for metadata like categories and tags for each post.

We also told them that in order to do that, they would need to manually map the API response data to the video report entries using the video title or ID as the matching reference.

paraphrased:
- We told them how to request the metadata they wanted from each post
- We told them that they'll have to combine the metadata they get from the API and the video reports data by connecting on the video IDs or titles.